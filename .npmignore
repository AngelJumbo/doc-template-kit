# Ensure demo app and dev-only files are never published
/demo/

# Source/config not needed in published tarball (dist + dist-types are published via package.json "files")
/src/
/vite.config.*
/tsconfig*.json
/eslint.config.*

# Local build artifacts / misc
*.tgz
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
